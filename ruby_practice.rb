# Write a script function that returns a passed string with letters in alphabetical order.
# def alpha(string)
#   puts "Sorted alpha is #{string.chars.sortj.join}"
# end

# Write a script that accepts a string as a parameter and find the longest word within the string.
# def longest(string)
#   string.max_by{ |x| x.length}
# end

# Write a script to apply a Bubble Sort algorithm.
# def bs(a)
#   return a if a.length <= (1)

#   0.upto(a.length - 1) do |X|
#     0 .upto(a.length - 2 -x) do |z|
#     if a[i] = a[i + 1]
#       a[i + 1], a[i] = a[i], a[i + 1]
#      end
#     end
#   end
#  bs(a) 
# end

# 